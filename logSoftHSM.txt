* Llamado a C_CloseAllSessions con un slotID(0) retornado por C_GetSlotList(CK_FALSE) y flag CKF_TOKEN_PRESENT apagado, debio arrojar CKR_TOKEN_NOT_PRESENT, pero arrojo CKR_OK

* Token con slotID(1), con 2 sesiones abiertas, 1 R/W abierta debio retornar en su info.ulRwSessionCount 1, pero retorno 2

* Llamada a C_Login con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , con tipo de usuario CKU_SO, y con un CKU_SO antes logueado, debio arrojar CKR_USER_ALREADY_LOGGED_IN, pero arrojo CKR_OK

* Llamada a C_Logout con una sesion R/O no logueada creada con slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, debio arrojar CKR_USER_NOT_LOGGED_IN, pero arrojo CKR_OK

* Llamada a C_Logout con una sesion R/W no logueada creada con slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, debio arrojar CKR_USER_NOT_LOGGED_IN, pero arrojo CKR_OK

* Llamada a C_Logout con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , con una sesion R/O y una R/W abiertas y recien deslogueadas, debio arrojar CKR_USER_NOT_LOGGED_IN, pero arrojo CKR_OK

* Llamado a C_CreateObject con sesion R/W abierta y logueada en normal user  n slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, y con un template que especifica CKA_TOKEN true y CKA_TOKEN false, debio arrojar CKR_TEMPLATE_INCOMPLETE, CKR_TEMPLATE_INCOMPLETE o CKR_TEMPLATE_INCOMPLETE, pero arrojo CKR_OK

* Llamado a C_DestroyObject con sesion R/O abierta y logueada en normal user  en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, al intentar destruir un objeto de token privado, debio arrojar CKR_SESSION_READ_ONLY, pero arrojo CKR_OBJECT_HANDLE_INVALID

* Llamado a C_DestroyObject con sesion R/O abierta y logueada en normal user  en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, al intentar destruir un objeto de token publico, debio arrojar CKR_SESSION_READ_ONLY, pero arrojo CKR_OBJECT_HANDLE_INVALID

* Llamado a C_DestroyObject con sesion R/O abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, al intentar destruir un objeto de token publico, debio arrojar CKR_SESSION_READ_ONLY, pero arrojo CKR_OBJECT_HANDLE_INVALID

* Llamado a C_GenerateKeyPair con sesion R/W abierta y logueada en normal user  en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, y con un template que especifica CKA_TOKEN true y CKA_TOKEN false, debio arrojar CKR_TEMPLATE_INCOMPLETE, CKR_TEMPLATE_INCOMPLETE o CKR_TEMPLATE_INCOMPLETE, pero arrojo CKR_OK

* Llamado a C_GenerateKeyPair con sesion R/W abierta y logueada en normal user  n slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, y con un template que  especifica en CKA_KEY_TYPE, CKK_DSA(y cuyo mecanismo es RSA), debio arrojar CKR_TEMPLATE_INCONSISTENT, pero arrojo CKR_OK

* Llamado a C_GenerateKeyPair con sesion R/W abierta y logueada en normal user  n slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, y con un template que  especifica en CKA_CLASS una invalida(8), debio arrojar CKR_TEMPLATE_INCONSISTENT, pero arrojo CKR_OK

* Llamado a C_GenerateKeyPair con sesion R/W abierta y logueada en normal user  n slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, y con un template que contiene un tipo de atributo invalido, debio arrojar CKR_ATTRIBUTE_TYPE_INVALID, pero arrojo CKR_OK

* Llamado a C_GenerateKeyPair con sesion R/O abierta y normal user logueado en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, debio arrojar CKR_SESSION_READ_ONLY, pero arrojo CKR_USER_NOT_LOGGED_IN

* Llamado a C_GenerateKeyPair con sesion R/W abierta y normal user logueado en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado, con parametro del mecanismo invalido CKR_MECHANISM_PARAM_INVALID, debio arrojar CKR_MECHANISM_PARAM_INVALID, pero arrojo CKR_OK

* Llamada a C_SignInit con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , con segundo argumento un mecanismo valido pero con parametros invalidos, debio arrojar CKR_MECHANISM_PARAM_INVALID, pero arrojo CKR_OK

* Llamada a C_SignInit con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , una llave que tiene en su atributo CKA_SIGN en CK_FALSE, debio arrojar CKR_KEY_FUNCTION_NOT_PERMITTED, pero arrojo CKR_OK

* Llamada a C_Sign con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , luego de una llamada erronea a C_Sign, debio arrojar CKR_OPERATION_NOT_INITIALIZED, pero arrojo CKR_OK

* Llamada a C_Sign con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , con el mecanismo CKM_RSA_PKCS, con un input mas largo que el maximo permitido por el mecanismo, debio arrojar CKR_DATA_LEN_RANGE, pero arrojo CKR_OK

* Llamada a C_DigestInit con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , con segundo argumento un mecanismo valido pero con parametros invalidos, debio arrojar CKR_MECHANISM_PARAM_INVALID, pero arrojo CKR_OK

* Llamada a C_Digest con una sesion R/W abierta en slotID(1) retornado por C_GetSlotList(CK_TRUE) y con flags CKF_TOKEN_INITIALIZED encendido y CKF_WRITE_PROTECTED apagado , luego de una llamada erronea a C_Digest, debio arrojar CKR_OPERATION_NOT_INITIALIZED, pero arrojo CKR_OK

